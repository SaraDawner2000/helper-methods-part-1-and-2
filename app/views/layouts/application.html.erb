<!DOCTYPE html>
<html>
  <head>
    <title>Helper Methods Part 1 and 2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <span><%= link_to "Movies", movies_path %></span>
      <span><%= link_to "Actors", actors_path %></span>
      <span><%= link_to "Roles", roles_path %></span>
    </nav>

    <hr>
    <% if notice.present? %>
      <div style="color:green;">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div style="color:red;">
        <%= alert %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
